[Unit]
Description=beacon-chain Service
After=network.target

[Service]
Type=simple
User=ubuntu
ExecStart=/bin/bash -c '/home/ubuntu/prysm/prysm.sh beacon-chain --p2p-host-ip=$(curl -s ident.me) --min-sync-peers 3 --datadir=/data/eth2'
Restart=always

[Install]
WantedBy=multi-user.target
